// /myActivity.ts

// Define the addition activity
export async function addTwoNumbers(a: number, b: number): Promise<number> {
    return a + b;
}

// Define the multiplication activity
export async function multiplyTwoNumbers(x: number, y: number): Promise<number> {
    return x * y;
}
